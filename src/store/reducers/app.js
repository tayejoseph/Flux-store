const initState = {
  loading: false,
}

const AppReducer = (state = initState, action) => {
  const { type, data } = action
  switch (type) {
    default:
      return state
  }
}

export default AppReducer
